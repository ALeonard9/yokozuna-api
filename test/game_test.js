// var chai = require('chai');
// var expect = require('chai').expect;
// var chaiAsPromised = require('chai-as-promised');
// chai.use(chaiAsPromised);
// chai.should();
// var game = require('../dist/game');
//
// describe('Allows calling of game functions', function() {
//   it('should create a new round', function() {
//     var test = game.newRound(1);
//     return expect(Promise.resolve(test)).to.eventually.have.property('insertId').should.eventually.be.an('number');
//   });
//   it('should simulate a round', function() {
//     var test = game.simDay(1, 1, 1);
//     return expect(Promise.resolve(test)).to.eventually.have.property('insertId').should.eventually.be.an('number');
//   });
// });
